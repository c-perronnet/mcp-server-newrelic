# Pinned to reduce supply-chain risk (avoid auto-pulling a future malicious release).
#
# fastmcp is bounded to the current major (3.x) to pick up security fixes while
# avoiding a future breaking major. Once you verify a specific version in your
# environment, prefer an exact pin (==) plus hashes via pip-compile /
# `pip install --require-hashes` for the strongest guarantee.
fastmcp>=3.4,<4
requests==2.32.5
